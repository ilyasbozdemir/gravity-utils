# Workspace hoisting for Windows
shamefully-hoist=true
public-hoist-pattern[]=*
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
# Use a single lockfile to keep it "general"
shared-workspace-lockfile=true
# Fix for potential Windows path issues
symlink=false
