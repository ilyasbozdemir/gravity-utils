{
  "name": "gravity-utils-workspace",
  "private": true,
  "scripts": {
    "web:dev": "pnpm -C web dev",
    "web:build": "pnpm -C web build",
    "desktop:prebuild": "powershell -Command \"if(Test-Path desktop/out){ Remove-Item -Recurse -Force desktop/out }; Copy-Item -Path web/out -Destination desktop/out -Recurse -Force\"",
    "desktop:build": "pnpm web:build && pnpm desktop:prebuild && pnpm -C desktop build",
    "desktop:clean": "pnpm -C desktop clean",
    "clean-all": "powershell -Command \"foreach($d in @('web/.next','web/out','desktop/dist','desktop/out')){ if(Test-Path $d){ Remove-Item -Recurse -Force $d } }\""
  }
}